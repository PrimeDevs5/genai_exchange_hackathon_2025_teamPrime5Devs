/**
 * Minimal PDF.js Worker Fallback
 * This is a simplified worker that attempts to provide basic functionality
 * when the main PDF.js worker fails to load
 */

// This is a minimal implementation - for production use the official PDF.js worker
console.log('📄 Using minimal PDF.js worker fallback');

// Basic worker message handling
self.onmessage = function(e) {
  console.warn('⚠️ Minimal PDF worker received message - limited functionality available');
  
  // Send back a basic response indicating limited functionality
  self.postMessage({
    type: 'error',
    error: new Error('PDF processing not fully available - using minimal worker')
  });
};